import { createUseStyles } from "react-jss";

export default createUseStyles({
    actions: {
        display: "flex",
        justifyContent: "center",
        marginTop: "15px"
    },
    pauseBtn: {
        marginRight: "10px"
    }
});